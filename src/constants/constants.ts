import { TFilterInitialState } from '../types/filter';

export const filterInitialState: TFilterInitialState = {
  title: '',
  category: 0,
  order: 'asc',
  sortBy: 'priceDefault',
};
export const countSkeleton = 8;
